import { LoginForm } from "@/components/auth/loginForm";

const Login = () => {
  return (
    <main>
      <div className="p-6">
        <LoginForm />
      </div>
    </main>
  );
};

export default Login;
