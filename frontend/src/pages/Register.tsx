import { RegisterForm } from "@/components/auth/registerForm";

const Register = () => {
  return (
    <main>
      <div className="p-6">
        <RegisterForm />
      </div>
    </main>
  );
};

export default Register;
